---
phase: 02-block-transfer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BLOCK-01, BLOCK-02, BLOCK-03]
must_haves:
  truths:
    - "Firmware file can be loaded from disk"
    - "SDO block download transfers firmware data"
    - "Sub-block transfers handle segments correctly"
    - "CRC is calculated and validated during transfer"
  artifacts:
    - path: "src/block_download.py"
      provides: "SDO block download module"
    - path: "src/firmware.py"
      provides: "Firmware loading and validation"
    - path: "tests/test_block_download.py"
      provides: "Block download unit tests"
  key_links:
    - from: "src/block_download.py"
      to: "src/auth.py"
      via: "Authentication required first"
    - from: "src/block_download.py"
      to: "canopen SdoClient"
      via: "Block transfer API"
---

<objective>
Implement SDO block download protocol for firmware transfer per CiA 301/302 and Delta-Q specifications.

Purpose: Transfer firmware to charger after authentication
Output: Working firmware download module
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-block-transfer/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create firmware loading module</name>
  <files>src/firmware.py</files>
  <action>
Create src/firmware.py with:
- FirmwareLoader class to load binary files
- Validate firmware file format
- Calculate file size for SDO header
- CRC calculation for firmware integrity
- Support for Delta-Q firmware file structure

Firmware loading should:
- Load binary file from specified path
- Calculate total size for SDO initiate message
- Compute CRC for end-of-transfer validation
  </action>
  <verify>
    <automated>python -c "from src.firmware import FirmwareLoader; print('Module loads OK')"</automated>
  </verify>
  <done>Firmware module can load and validate firmware files</done>
</task>

<task type="auto">
  <name>Task 2: Create SDO block download module</name>
  <files>src/block_download.py</files>
  <action>
Create src/block_download.py with:
- SDOBlockDownload class using canopen library
- Block download initiate (0x1F50:01)
- Sub-block segment handling
- Block download end with CRC
- Progress callback support
- Error handling and retry logic

Using canopen's SdoClient:
- node.sdo.open() with block_transfer=True
- File-like interface for streaming data
- Handle first block (15s erase time)
- Subsequent blocks faster (~hundreds of ms)
  </action>
  <verify>
    <automated>python -c "from src.block_download import SDOBlockDownload; print('Module loads OK')"</automated>
  </verify>
  <done>Block download module can transfer firmware</done>
</task>

<task type="auto">
  <name>Task 3: Create block download unit tests</name>
  <files>tests/test_block_download.py</files>
  <action>
Create tests/test_block_download.py with:
- Test firmware file loading
- Test firmware size calculation
- Test CRC calculation
- Test block download message building
- Mock canopen for testing without hardware
  </action>
  <verify>
    <automated>python -m pytest tests/test_block_download.py -v</automated>
  </verify>
  <done>Unit tests pass for block download module</done>
</task>

</tasks>

<verification>
- [ ] src/firmware.py created with loading logic
- [ ] src/block_download.py created with SDO transfer
- [ ] tests/test_block_download.py created with tests
- [ ] CRC calculation verified
- [ ] All tests pass
</verification>

<success_criteria>
BLOCK-01: SDO block transfer protocol - IMPLEMENTED
BLOCK-02: Sub-block transfer handling - IMPLEMENTED  
BLOCK-03: CRC calculation and validation - IMPLEMENTED

Ready to proceed to Phase 3 (Error Handling)
</success_criteria>

<output>
After completion, create `.planning/phases/02-block-transfer/02-01-SUMMARY.md`
</output>