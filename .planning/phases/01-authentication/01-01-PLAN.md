---
phase: 01-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "SDO authentication sends seed request and receives seed value"
    - "Key is calculated from seed + customer secret"
    - "Key response is sent and validated"
    - "Authentication success/failure is clearly reported"
  artifacts:
    - path: "src/auth.py"
      provides: "SDO authentication module"
    - path: "src/sdo.py"
      provides: "SDO protocol helpers"
    - path: "tests/test_auth.py"
      provides: "Authentication unit tests"
  key_links:
    - from: "src/auth.py"
      to: "python-can bus"
      via: "send/receive CAN frames"
    - from: "src/auth.py"
      to: "config.json"
      via: "customer_secret loading"
---

<objective>
Implement SDO authentication sequence with seed/key exchange per CiA 301/302 and Delta-Q specifications.

Purpose: Establish secure communication channel before firmware download
Output: Working authentication module that can validate against Delta-Q charger
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SDO authentication module</name>
  <files>src/auth.py</files>
  <action>
Create src/auth.py with:
- SDOUploadInitiate function to request seed (object 0x2400:01)
- Key calculation algorithm (translate from Delta-Q C code to Python)
- SDODownloadInitiate function to send key (object 0x2400:02)
- Authentication class that ties it all together
- Proper timeout handling (1-2 seconds per CONTEXT.md)
- Config file loading for customer secret

Key algorithm from Delta-Q doc (C code to translate):
- Input: seed + customer_secret (lower 16 bits)
- Output: 16-bit key
- Uses NAND and XOR bit operations in a loop
  </action>
  <verify>
    <automated>python -c "from src.auth import authenticate; print('Module loads OK')"</automated>
  </verify>
  <done>Authentication module can send seed request, calculate key, send response</done>
</task>

<task type="auto">
  <name>Task 2: Create SDO protocol helpers</name>
  <files>src/sdo.py</files>
  <action>
Create src/sdo.py with:
- CANopen SDO message building functions
- Parse SDO response frames
- Handle SDO Abort transfers (error handling)
- Constants for CANopen objects used:
  - 0x2400:01 (Seed)
  - 0x2400:02 (Key)
  - 0x1F51:01 (Program Control)
</action>
  <verify>
    <automated>python -c "from src.sdo import SDORequest; print('SDO module loads OK')"</automated>
  </verify>
  <done>SDO helper functions can build request frames and parse responses</done>
</task>

<task type="auto">
  <name>Task 3: Create authentication unit tests</name>
  <files>tests/test_auth.py</files>
  <action>
Create tests/test_auth.py with:
- Test key calculation algorithm (known test vectors)
- Test seed request message building
- Test key response message building
- Test config file loading
- Mock CAN bus for testing without hardware

Test key calculation with:
- Known seed + secret → expected key
  </action>
  <verify>
    <automated>python -m pytest tests/test_auth.py -v</automated>
  </verify>
  <done>Unit tests pass for authentication module functions</done>
</task>

</tasks>

<verification>
- [ ] src/auth.py created with authentication logic
- [ ] src/sdo.py created with SDO protocol helpers
- [ ] tests/test_auth.py created with unit tests
- [ ] Key calculation algorithm verified against known values
- [ ] Config file loading tested
- [ ] All tests pass
</verification>

<success_criteria>
AUTH-01: SDO authentication sequence with seed/key exchange - IMPLEMENTED
AUTH-02: Key validation logic - IMPLEMENTED

Ready to proceed to Phase 2 (Block Transfer)
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-01-SUMMARY.md`
</output>