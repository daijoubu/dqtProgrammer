# Memory Index

- [Cross-platform python-can config plan](project_cross_platform.md) — Future phase replaces socketcan `-iface` shim with python-can .ini config for Windows support
- [canopen Network(bus) requires connect()](feedback_canopen_network_bus.md) — Network(bus) sets self.bus but skips Notifier creation; must call connect() separately
